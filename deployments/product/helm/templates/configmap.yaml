apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "product.fullname" . }}-config
  labels:
    {{- include "product.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"