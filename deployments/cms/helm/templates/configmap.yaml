apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cms.fullname" . }}-config
  labels:
    {{- include "cms.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"