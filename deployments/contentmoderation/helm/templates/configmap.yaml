apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "contentmoderation.fullname" . }}-config
  labels:
    {{- include "contentmoderation.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"