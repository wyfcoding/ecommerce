apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "coupon.fullname" . }}-config
  labels:
    {{- include "coupon.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"