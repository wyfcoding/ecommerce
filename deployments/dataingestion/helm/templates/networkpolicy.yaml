apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: { include "dataingestion.fullname" . }
  labels:
    {- include "dataingestion.labels" . | nindent 4 }
spec:
  podSelector:
    matchLabels:
      {- include "dataingestion.selectorLabels" . | nindent 6 }
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
