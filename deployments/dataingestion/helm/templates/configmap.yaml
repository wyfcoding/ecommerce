apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dataingestion.fullname" . }}-config
  labels:
    {{- include "dataingestion.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"