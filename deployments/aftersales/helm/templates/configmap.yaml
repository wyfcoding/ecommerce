apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aftersales.fullname" . }}-config
  labels:
    {{- include "aftersales.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"