apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pointsmall.fullname" . }}-config
  labels:
    {{- include "pointsmall.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"