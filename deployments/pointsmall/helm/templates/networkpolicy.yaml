apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: { include "pointsmall.fullname" . }
  labels:
    {- include "pointsmall.labels" . | nindent 4 }
spec:
  podSelector:
    matchLabels:
      {- include "pointsmall.selectorLabels" . | nindent 6 }
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
