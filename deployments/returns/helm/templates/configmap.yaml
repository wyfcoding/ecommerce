apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "returns.fullname" . }}-config
  labels:
    {{- include "returns.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"