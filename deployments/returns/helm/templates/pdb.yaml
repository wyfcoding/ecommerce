apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "returns.fullname" . }}
  labels:
    {{- include "returns.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "returns.selectorLabels" . | nindent 6 }}