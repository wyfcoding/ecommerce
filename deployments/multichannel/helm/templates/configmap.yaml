apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "multichannel.fullname" . }}-config
  labels:
    {{- include "multichannel.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"