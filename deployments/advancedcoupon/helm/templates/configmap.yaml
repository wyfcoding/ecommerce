apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "advancedcoupon.fullname" . }}-config
  labels:
    {{- include "advancedcoupon.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"