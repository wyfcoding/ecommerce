{- if .Values.serviceAccount.create -}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: { include "settlement.serviceAccountName" . }
  labels:
    {- include "settlement.labels" . | nindent 4 }
  {- with .Values.serviceAccount.annotations }
  annotations:
    {- toYaml . | nindent 4 }
  {- end }
{- end }
