apiVersion: v1
kind: ConfigMap
metadata:
  name: { include "settlement.fullname" . }-config
  labels:
    {- include "settlement.labels" . | nindent 4 }
data:
  # Placeholder for config.toml content or other config
  config.toml: |
    [server]
    name = "{ .Values.image.repository }"
    environment = "prod"
