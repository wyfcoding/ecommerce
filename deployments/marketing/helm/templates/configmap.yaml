apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "marketing.fullname" . }}-config
  labels:
    {{- include "marketing.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"