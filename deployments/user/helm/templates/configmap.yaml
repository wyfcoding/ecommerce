apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "user.fullname" . }}-config
  labels:
    {{- include "user.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"