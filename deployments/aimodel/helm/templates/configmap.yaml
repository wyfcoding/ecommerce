apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aimodel.fullname" . }}-config
  labels:
    {{- include "aimodel.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"