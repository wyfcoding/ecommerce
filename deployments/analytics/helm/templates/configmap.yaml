apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "analytics.fullname" . }}-config
  labels:
    {{- include "analytics.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"