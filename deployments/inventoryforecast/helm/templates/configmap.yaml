apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "inventoryforecast.fullname" . }}-config
  labels:
    {{- include "inventoryforecast.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"