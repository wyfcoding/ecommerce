apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: { include "inventoryforecast.fullname" . }
  labels:
    {- include "inventoryforecast.labels" . | nindent 4 }
spec:
  podSelector:
    matchLabels:
      {- include "inventoryforecast.selectorLabels" . | nindent 6 }
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
