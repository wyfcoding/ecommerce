apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kyc.fullname" . }}-config
  labels:
    {{- include "kyc.labels" . | nindent 4 }}
data:
  APP_NAME: {{ .Chart.Name }}
  APP_ENV: {{ .Values.env | first | index "value" }}
