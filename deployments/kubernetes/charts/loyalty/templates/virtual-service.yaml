apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: loyalty
spec:
  hosts:
  - "*"
  gateways:
  - ecommerce-gateway
  http:
  - match:
    - uri:
        prefix: "/api/loyalty"
    route:
    - destination:
        host: loyalty
        port:
          number: 80
