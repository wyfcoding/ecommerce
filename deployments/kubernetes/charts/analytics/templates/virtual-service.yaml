apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: analytics
spec:
  hosts:
  - "*"
  gateways:
  - ecommerce-gateway
  http:
  - match:
    - uri:
        prefix: "/api/analytics"
    route:
    - destination:
        host: analytics
        port:
          number: 80
