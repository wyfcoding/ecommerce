apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: coupon
spec:
  hosts:
  - "*"
  gateways:
  - ecommerce-gateway
  http:
  - match:
    - uri:
        prefix: "/api/coupon"
    route:
    - destination:
        host: coupon
        port:
          number: 80
