apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: flashsale
spec:
  hosts:
  - "*"
  gateways:
  - ecommerce-gateway
  http:
  - match:
    - uri:
        prefix: "/api/flashsale"
    route:
    - destination:
        host: flashsale
        port:
          number: 80
