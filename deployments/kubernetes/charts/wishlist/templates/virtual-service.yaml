apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: wishlist
spec:
  hosts:
  - "*"
  gateways:
  - ecommerce-gateway
  http:
  - match:
    - uri:
        prefix: "/api/wishlist"
    route:
    - destination:
        host: wishlist
        port:
          number: 80
