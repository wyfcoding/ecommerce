apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: pricing
spec:
  hosts:
  - "*"
  gateways:
  - ecommerce-gateway
  http:
  - match:
    - uri:
        prefix: "/api/pricing"
    route:
    - destination:
        host: pricing
        port:
          number: 80
