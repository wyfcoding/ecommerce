apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: notification
spec:
  hosts:
  - "*"
  gateways:
  - ecommerce-gateway
  http:
  - match:
    - uri:
        prefix: "/api/notification"
    route:
    - destination:
        host: notification
        port:
          number: 80
