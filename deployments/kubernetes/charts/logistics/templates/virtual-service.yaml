apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: logistics
spec:
  hosts:
  - "*"
  gateways:
  - ecommerce-gateway
  http:
  - match:
    - uri:
        prefix: "/api/logistics"
    route:
    - destination:
        host: logistics
        port:
          number: 80
