apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cms
spec:
  hosts:
  - "*"
  gateways:
  - ecommerce-gateway
  http:
  - match:
    - uri:
        prefix: "/api/cms"
    route:
    - destination:
        host: cms
        port:
          number: 80
