apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cdc
spec:
  hosts:
  - "*"
  gateways:
  - ecommerce-gateway
  http:
  - match:
    - uri:
        prefix: "/api/cdc"
    route:
    - destination:
        host: cdc
        port:
          number: 80
