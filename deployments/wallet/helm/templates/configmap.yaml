apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wallet.fullname" . }}-config
  labels:
    {{- include "wallet.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"