apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "audit.fullname" . }}-config
  labels:
    {{- include "audit.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"