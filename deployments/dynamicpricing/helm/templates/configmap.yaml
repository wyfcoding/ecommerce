apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dynamicpricing.fullname" . }}-config
  labels:
    {{- include "dynamicpricing.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"