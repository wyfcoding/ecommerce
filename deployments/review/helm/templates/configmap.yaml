apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "review.fullname" . }}-config
  labels:
    {{- include "review.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"