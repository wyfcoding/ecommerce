apiVersion: v1
kind: Service
metadata:
  name: {{ include "influencer.fullname" . }}
  labels:
    {{- include "influencer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "influencer.selectorLabels" . | nindent 4 }}
