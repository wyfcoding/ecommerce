apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "financialsettlement.fullname" . }}-config
  labels:
    {{- include "financialsettlement.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"