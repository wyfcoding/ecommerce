apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "payment.fullname" . }}-config
  labels:
    {{- include "payment.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"