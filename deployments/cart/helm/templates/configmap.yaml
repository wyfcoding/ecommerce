apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cart.fullname" . }}-config
  labels:
    {{- include "cart.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"