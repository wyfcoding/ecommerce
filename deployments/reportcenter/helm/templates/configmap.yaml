apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "reportcenter.fullname" . }}-config
  labels:
    {{- include "reportcenter.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"