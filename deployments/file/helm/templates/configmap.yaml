apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "file.fullname" . }}-config
  labels:
    {{- include "file.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"