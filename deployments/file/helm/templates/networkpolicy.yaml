apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: { include "file.fullname" . }
  labels:
    {- include "file.labels" . | nindent 4 }
spec:
  podSelector:
    matchLabels:
      {- include "file.selectorLabels" . | nindent 6 }
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
