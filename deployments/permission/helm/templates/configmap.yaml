apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "permission.fullname" . }}-config
  labels:
    {{- include "permission.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"