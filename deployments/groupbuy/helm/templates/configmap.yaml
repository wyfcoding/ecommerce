apiVersion: v1
kind: ConfigMap
metadata:
  name: { include "groupbuy.fullname" . }-config
  labels:
    {- include "groupbuy.labels" . | nindent 4 }
data:
  # Placeholder for config.toml content or other config
  config.toml: |
    [server]
    name = "{ .Values.image.repository }"
    environment = "prod"
