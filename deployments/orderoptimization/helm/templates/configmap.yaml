apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "orderoptimization.fullname" . }}-config
  labels:
    {{- include "orderoptimization.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"