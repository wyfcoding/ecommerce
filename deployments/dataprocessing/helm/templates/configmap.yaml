apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dataprocessing.fullname" . }}-config
  labels:
    {{- include "dataprocessing.labels" . | nindent 4 }}
data:
  config.toml: |
    [server]
    name = "{{ .Values.image.repository }}"
    environment = "prod"